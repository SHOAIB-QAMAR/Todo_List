services:
  - type: web
    name: Todo List
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview -- --host 0.0.0.0 --port 10000
    envVars:
      - key: NODE_VERSION
        value: 18
    autoDeploy: true
